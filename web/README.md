Run chrome with shared memory enabled

```shell
open -a "Google Chrome" -n --args --user-data-dir=/Users/ingun/insecure-chrome --disable-web-security --enable-features=SharedArrayBuffer --shared-array-buffer-unrestricted-access-allowed
```

